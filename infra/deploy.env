# Deploy mode + host parameters. Read by scripts/deploy.sh.
#
# Flip DEPLOY_MODE here, commit, run ./scripts/deploy.sh. That's the switch.

DEPLOY_MODE=pre-launch
CADDY_HOST=caddy.lan
PI_HOST=pi.lan
CADDY_WEBROOT=/srv/telescope
