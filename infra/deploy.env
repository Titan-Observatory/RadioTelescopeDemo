# Host parameters for the bare-metal systemd deploy.
# The Docker compose path doesn't use this file — see ../deploy.sh.

HARDWARE_HOST=pi.lan          # runs rt-hardware.service
PLATFORM_HOST=platform.lan    # runs rt-platform.service + serves the frontend
CADDY_HOST=caddy.lan          # TLS terminator in front of PLATFORM_HOST:8000
