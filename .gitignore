__pycache__/
*.py[cod]
.pytest_cache/
.pytest-tmp/
*.egg-info/
*.log
*.jsonl

# Local config and runtime artefacts. The template `config.example.toml` is
# tracked; copy it to `config.toml` on first run and edit there.
config.toml
spectrum_baseline.json
events.json
passwords.txt

# Local assistant/tooling files. Keep these out of the public repository.
CLAUDE.md
AGENTS.md
.agents/
.claude/
.codex/
.cursor/
.continue/
.aider*
.windsurf/
.roo/
